name: Android CI

on:
  push:
    branches:
      - main
    paths:
      - "packages/android/**"
  pull_request:
    paths:
      - "packages/android/**"
      - ".github/workflows/ci-android.yml"
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    working-directory: packages/android

jobs:
  build:
    name: "Android CI (JDK ${{ matrix.jdk-version }})"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        jdk-version: ["17", "21"] # 17 is the AGP 8.x minimum; 21 is the current LTS ceiling

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v4
        with:
          distribution: corretto
          java-version: ${{ matrix.jdk-version }}

      - uses: gradle/actions/setup-gradle@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4.7"
          bundler-cache: true

      - name: Install Fastlane
        run: bundle install

      - name: Build connector
        run: ./gradlew connector:assembleRelease

      - name: Lint
        run: ./gradlew connector:lint

      - name: Unit tests
        run: ./gradlew connector:test

      - name: Build example app
        run: bundle exec fastlane android build_example_app
