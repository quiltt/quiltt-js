import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'dev.quiltt.examples.capacitorvue',
  appName: 'Quiltt Capacitor Vue Example',
  webDir: 'dist',
}

export default config
