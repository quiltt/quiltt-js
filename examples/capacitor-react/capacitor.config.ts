import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'dev.quiltt.examples.capacitorreact',
  appName: 'Quiltt Capacitor React Example',
  webDir: 'dist',
}

export default config
